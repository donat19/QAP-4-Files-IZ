def format_currency(value):
    """Formats a number as currency."""
    return f"${value:,.2f}"

def format_title_case(value):
    """Formats a string in Title Case."""
    return value.title()

def format_uppercase(value):
    """Formats a string in uppercase."""
    return value.upper()
